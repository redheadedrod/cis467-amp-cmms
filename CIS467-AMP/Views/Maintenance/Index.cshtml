@{
    /**
     * Author: Jason Bensel
     * Stockroom main page
     */

    //Sets tab bar title
    ViewBag.Title = "Maintenance";

    //Uses this scaffolding as main layout --
    //can change later, but the below cshtml is called by RenderBody() in _layout.cshmtl
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col col-md-2">
        <ul class="nav nav-pills nav-stacked">
            <li>@Html.ActionLink("Maintenance Home", "Index")</li>
            <li>@Html.ActionLink("Current Work Orders", "CurrentWorkOrder")</li>
            <li>@Html.ActionLink("Create Work Order", "WorkOrder")</li>
        </ul>
    </div>
    <div class="col-md-10">
        <h1 id="title">@ViewBag.Title</h1>
    </div>
</div>