@model IEnumerable<CIS467_AMP.Models.Logbook.LogbookStockroomOrder>
@{
    /**
     * Author: Rodney Fulk
     * Stockroom orders logbook
     */

    //Sets tab bar title
    ViewBag.Title = "Stockroom Orders";

    //Uses this scaffolding as main layout --
    //can change later, but the below cshtml is called by RenderBody() in _layout.cshmtl
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    @Html.Partial("_LogbookLeftNavBar") @*Could put a model as second argument*@
    <div class="col-md-10">
        <h1 id="titleS">@ViewBag.Title</h1>
    </div>
    <div>
        @*Following Here for notes more than anything*@
        @*@{
                var className = Model.MoreText.Count > 1 ? "popular" : null;

            }*@

        @*<h2 class ="@className">@Model.Text</h2> *@
        @* <ul>
                @foreach (var order in Model)
                {
                    <li>@order.Order</li>

                }
            </ul>*@
        <div class="col col-md-10">
            <table class="table">
                <tr>
                    <th>Date Ordered</th>
                    <th>Orderd by</th>
                    <th>Asset</th>
                    <th>Order Number</th>
                    <th>Order Status</th>
                    <th>Status Updated</th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.EnteredDateTime)</td>
                        <td>@Html.DisplayFor(modelItem => item.Worker.LastName), @Html.DisplayFor(modelItem => item.Worker.FirstName)</td>
                        <td>@Html.DisplayFor(modelItem => item.AssetInventory.Name)</td>
                        <td>@Html.DisplayFor(modelItem => item.Order.OrderNumber)</td>
                        <td>@Html.DisplayFor(modelItem => item.Order.OrderStatus.Name)</td>
                        <td>@Html.DisplayFor(modelItem => item.Order.StatusLastUpDate)</td>
                    </tr>
                }
            </table>
        </div>
    </div>
</div>
